# Latin Diachronic Semantic Shift Analysis: Classical vs. Aquinas

Этот репозиторий содержит инструментарий для анализа диахронических семантических изменений в латинском языке. С помощью сравнения векторных моделей (Word Embeddings) мы исследуем, как трансформировались значения ключевых понятий при переходе от классической античности к эпохе схоластики (на материале трудов Фомы Аквинского).

Данные (выровненные векторные модели) предоставлены проектом [LiLa: Linking Latin](https://lila-erc.eu/).

## 📂 Описание

В репозитории представлен единый Jupyter Notebook:
**`latin_semantic_shift_analysis.ipynb`**

Этот скрипт выполняет полный цикл исследования («end-to-end pipeline»), который состоит из двух логических этапов:

### 1. Глобальный анализ (Global Statistics)

Оценивает масштаб изменений по всему общему словарю двух корпусов.

* **Вычисления:** Рассчитывает косинусное сходство (Cosine Similarity) для всех общих лемм.
* **Статистика:** Вычисляет параметры распределения сдвигов ($\mu$, $\sigma$, медиана) и определяет пороги семантической стабильности на основе Z-оценок (интервалы $\pm 1\sigma$, $\pm 2\sigma$).
* **Результаты:**
    * Гистограмма распределения сдвигов (`semantic_shift_histogram_combined.png`).
    * Списки топ-10 самых стабильных и самых изменившихся слов.
    * Полная таблица данных в CSV (`classical_aquinas_cosine_full.csv`).


### 2. Детальный анализ (Focused Analysis)

Фокусируется на списке ключевых концептов (*populus, plebs, natio, ecclesia, fides, lex* и др.) для качественной интерпретации.

* **Визуализация:**
    * Бар-чарт семантических сдвигов с точными значениями (`focused_words_barchart.png`).
    * Тепловые карты семантической близости и матрица изменений (`heatmap_*.png`).
    * Проекция семантического пространства через PCA (`viz_pca.png`).
* **Контекст:**
    * Генерация текстового отчета о ближайших соседях (Nearest Neighbors) для каждого ключевого слова (`nearest_neighbors_report.txt`).


## 🚀 Как использовать

Скрипт полностью готов к запуску в среде **Google Colab** или локально.

### Инструкция

1. Откройте файл `latin_semantic_shift_analysis.ipynb`.
2. Запустите ячейки последовательно.
3. **Автоматическая загрузка:** Скрипт сам скачает необходимые файлы моделей (`OperaLatina.vec.txt`, `OperaMaiora.vec.txt`) с сервера LiLa ERC, если они отсутствуют.

### Зависимости (Requirements)

Для работы необходим Python 3.x и следующие библиотеки:

```bash
pip install gensim numpy pandas matplotlib seaborn scikit-learn tqdm
```


## 📊 Методология

Исследование опирается на дистрибутивную семантику и статистический анализ:

1. **Косинусное сходство:** Используется как метрика семантической близости. Диапазон значений от `1.0` (идентичность) до `-1.0` (противоположность).
2. **Статистические пороги:** Для классификации сдвигов используются параметры нормального распределения:
    * **Нормальный дрейф:** значения в пределах $\mu \pm 1\sigma$ (охватывает ~68% лексики).
    * **Значимый сдвиг:** отклонение более чем на $1\sigma$.
    * **Экстремальный сдвиг:** отклонение более чем на $2\sigma$ (охватывает ~5% лексики).

## 📝 Выходные данные

После выполнения скрипта в рабочей директории появятся:

* `*.png` — визуализации (гистограммы, карты, графики).
* `*.csv` — таблицы с метриками сходства.
* `*.txt` — отчеты по ближайшим соседям.