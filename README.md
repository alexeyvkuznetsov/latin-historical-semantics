# Latin Diachronic Semantic Shift Analysis: Classical vs. Aquinas

Этот репозиторий содержит инструменты для анализа семантических изменений (сдвигов значений) в латинском языке путем сравнения векторных моделей (Word Embeddings).

Мы сопоставляем модель **Классической латыни** (`OperaLatina`) с моделью языка **Фомы Аквинского** (`OperaMaiora`), чтобы выявить, как изменилось значение ключевых понятий (например, *populus*, *fides*, *lex*) в эпоху схоластики.

Данные (векторные модели) взяты из проекта [LiLa: Linking Latin](https://lila-erc.eu/).

## 📂 Структура репозитория

В репозитории находятся два основных Jupyter Notebook, каждый из которых решает свои задачи:

### 1. `calc_semantic_shift_stats.ipynb` (Глобальная статистика)

Этот скрипт предназначен для "взгляда сверху" на весь словарь.

* **Что делает:**
    * Загружает модели.
    * Вычисляет косинусное сходство (Cosine Similarity) для **всех** общих слов между двумя эпохами.
    * Рассчитывает статистические параметры распределения ($\mu$ — среднее, $\sigma$ — стандартное отклонение).
    * Определяет пороги семантической стабильности и сдвига (на основе интервалов $\pm 1\sigma$ и $\pm 2\sigma$).
    * Выводит списки топ-10 самых стабильных и самых изменившихся слов.
* **Результат:**
    * `semantic_shift_histogram_combined.png` — гистограмма распределения сдвигов.
    * `classical_aquinas_cosine_full.csv` — полная таблица данных.


### 2. `analyze_semantic_shift_details.ipynb` (Детальный анализ)

Этот скрипт работает как "микроскоп", фокусируясь на конкретных списках слов (*Core Concepts*, *Semantic Anchors*).

* **Что делает:**
    * Анализирует список избранных понятий (например: *populus, plebs, natio, ecclesia, fides, lex* и др.).
    * Строит визуализации для конкретных слов.
    * Находит ближайших соседей (Nearest Neighbors) для понимания контекста.
* **Результат:**
    * `focused_words_barchart.png` — график сдвигов для выбранных слов с точными значениями.
    * `heatmap_*.png` — тепловые карты семантической близости внутри эпох и карта изменений (*Difference Matrix*).
    * `viz_pca.png` — визуализация семантического пространства (PCA проекция).
    * `nearest_neighbors_report.txt` — текстовый отчет о ближайших соседях.


## 🚀 Как использовать

Скрипты адаптированы для запуска в среде **Google Colab**, но могут работать и локально.

### Запуск

1. Откройте нужный файл (`.ipynb`).
2. Выполняйте ячейки последовательно.
3. **Важно:** Вам не нужно скачивать модели вручную. Скрипты автоматически загрузят необходимые файлы (`.vec.txt`) с сервера LiLa ERC, если их нет в рабочей директории.

### Требования (Dependencies)

Для работы необходим Python 3 и следующие библиотеки:

```bash
pip install gensim numpy pandas matplotlib seaborn scikit-learn tqdm
```


## 📊 Методология

В основе анализа лежит гипотеза о том, что распределение семантических сдвигов в языке близко к нормальному (Гауссову).

1. **Косинусное сходство (Cosine Similarity):** Мера близости двух векторов. Значение `1.0` означает идентичность, значения близкие к `0` или отрицательные указывают на сильный семантический сдвиг или ортогональность смыслов.
2. **Пороги (Z-scores):** Мы не используем произвольные числа для оценки "силы" сдвига. Вместо этого мы вычисляем среднее ($\mu$) и отклонение ($\sigma$) по всему словарю.
    * **Нормальный дрейф:** значения в пределах $\mu \pm 1\sigma$.
    * **Выраженный сдвиг:** значения ниже $\mu - 1\sigma$.
    * **Экстремальный сдвиг:** значения ниже $\mu - 2\sigma$.

## 📝 Результаты и интерпретация

В ходе анализа создаются CSV-файлы с расчетами и PNG-изображения.

* **Гистограмма** в первом скрипте позволяет оценить общий масштаб изменений между эпохами.
* **Тепловые карты** во втором скрипте показывают, как перестраивались связи внутри семантических полей (например, как *ecclesia* отдаляется от античных терминов и сближается с *christianus*).